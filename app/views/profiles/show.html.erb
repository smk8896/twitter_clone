<!-- Username -->
<h1><%= @user.username %>'s Profile</h1>

<!-- Show all their tweets -->
<% @user.tweets.each do |tweet| %>
  <%= tweet.message %> <br>
<% end %>

<%= link_to "Follow", relationships_path(friend_id: @user.id), method: :post %>